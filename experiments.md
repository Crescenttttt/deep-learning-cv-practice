# 实验登记表（YOLOv10 + TT100K）

> **用途**：每跑一组训练就在此登记一行，作为报告对比表的数据源（任务 2.3.4 / 2.3.5）。
> **日志规范**：每次训练输出到 `runs/detect/<name>/`，`<name>` 与下表「实验名」一致；
> 训练用 `scripts/train.py`，评测用 `scripts/eval.py --weights runs/detect/<name>/weights/best.pt`。
> 数据集固定：TT100K 45 类，train/val/test = 5493 / 610 / 3067（见 `docs/数据下载与目录说明.md`）。

## 计划实验（待跑，结果列填实测值）

| 实验名 | 模型 | imgsz | batch | lr0 | epochs | 增广 | mAP@0.5 | mAP@0.5:0.95 | 备注 |
|--------|------|:---:|:---:|:---:|:---:|------|:---:|:---:|------|
| `s_base_640` | YOLOv10-S | 640 | 16 | auto | 100 | 默认(mosaic+关10轮) | **0.415** | **0.302** | ✅ **主实验 baseline**（test 集），未达 0.70，小目标瓶颈明显 |
| `s_640_sgd` | YOLOv10-S | 640 | 16 | 0.01(SGD) | 100 | 默认 | **0.646** | **0.494** | ✅ **优化器调参**（test）：显式 SGD 替 auto，比 640 baseline **+0.231(+56%)**，证实 baseline 低分是欠拟合（3.2.1） |
| `s_imgsz1280` | YOLOv10-S | 1280 | 4 | auto | 100 | 默认 | **0.592** | **0.457** | ✅ **高分辨率调参**（test）：比 640 baseline +0.177，验证小目标瓶颈（3.2.2） |
| ~~`s_1280_sgd_e150`~~ | YOLOv10-S | 1280 | 8 | 0.01(SGD) | 150 | 默认 | 0.771 | 0.601 | ⚠️ **已被 resume bug 阉割、由 clean 版取代**：e40 中断后 `--resume` 未带 `--imgsz 1280`，后 110 轮实为 640，best.pt 仅含 40 个有效 1280 epoch。详见「🔴 重大修正」 |
| **`s_1280_sgd_e150_clean`** | YOLOv10-S | 1280 | 6 | 0.01(SGD) | 150 | 默认 | **0.921** | **0.735** | ✅✅ **最终主实验（修正版）**（test）：全程不中断，val 爬到 0.935@e143 未崩。比旧版 **+0.150**、比 baseline **+0.506(+122%)**，**超过 v8-S 640（0.798）**；test≈val 无泄漏 |
| `s_nomosaic` | YOLOv10-S | 640 | 16 | 0.01(SGD) | 100 | **全程关 mosaic**(--mosaic 0) | **0.629** | **0.477** | ✅ **增广消融**（test）：对照 `s_640_sgd`（仅 mosaic 不同）0.646/0.494 → 关 mosaic **−0.017**，证明 mosaic 有正贡献（3.2.3） |
| `v8s_base_640` | YOLOv8-S | 640 | 16 | auto | 100 | 默认 | **0.798** | **0.610** | ✅ **横向对比**（test）：同条件 v8 显著高于 v10 baseline，根因是 v10 欠拟合（见下）（3.3.1） |
| `s_640_sgd_e150` | YOLOv10-S | 640 | 16 | 0.01(SGD) | 150 | 默认 | **0.706** | **0.542** | ✅ **v8 公平对比补充**（test）：150 轮基本饱和（val 0.728@e145），同 640 条件**仍低于 v8 0.798**——「加 epoch 追平」猜想证伪，详见「公平对比补充」（3.3.1 补充） |
| `n_base_640` | YOLOv10-N | 640 | 32 | 0.01 | 100 | 默认 | — | — | （可选）轻量版对照 |

> 及格线（任务 1.3.3，先调研后定）：mAP@0.5 ≥ 0.70。达不到则进入调参循环。
> batch 视 12GB 显存调整：640 通常 16 可行，800 建议降到 8。1280 实测 batch4 仅占约半显存（~6GB），可提到 **batch 8（workers 4）** 更充分利用、训练吞吐近翻倍；峰值因 mosaic 波动，盯 `nvidia-smi`，冲到 ~11GB+ 则降回 6。

## 已完成实验记录

| 日期 | 实验名 | mAP@0.5 | mAP@0.5:0.95 | 参数量 | 延迟(ms) | 结论 / 备注 |
|------|--------|:---:|:---:|:---:|:---:|------|
| 2026-06-02 | `s_base_640` | 0.415 | 0.302 | 8.1M / 25.0 GFLOPs | 4.2 | test 集；mP 0.427 / mR 0.445。弱类全是小尺寸限速/限高标志(pl20/ph5/pm30 AP<0.1)，强类为大图案(il100/pl100 AP>0.55)。**小目标是瓶颈** → 下一步提 imgsz 到 1280 |
| 2026-06-03 | `s_imgsz1280` | **0.592** | **0.457** | 8.1M / 25.0 GFLOPs | — | test 集；mP 0.526 / mR 0.601。**比 640 baseline +0.177 mAP@0.5(+43%)**，强类升至 pl120 0.78/pr40 0.75，弱类仍为 ph4/pl20/ph5 但已翻倍。验证「提分辨率攻小目标」。batch4+workers2 稳定，~6h。 |
| 2026-06-04 | `v8s_base_640` | **0.798** | **0.610** | ~11.2M / 28.6 GFLOPs | — | **横向对比对照**；test 集，mP 0.791 / mR 0.732。同数据/同 640/同 100 轮，远高于 v10 baseline 0.415。**注意：这不是「v8 强于 v10」，而是 v10 在 auto 超参下严重欠拟合**（见下「优化器发现」）。 |
| 2026-06-04 | `s_640_sgd` | **0.646** | **0.494** | 8.07M / 24.6 GFLOPs | 3.7 | test 集，mP 0.610 / mR 0.623。**仅把 optimizer=auto 换成显式 SGD lr=0.01，640 下 +0.231 mAP@0.5(+56%)**。val 曲线 100 轮仍陡升未饱和（25/50/75/100 = 0.367/0.495/0.612/0.651），说明仍欠拟合，加 epoch 还能涨。弱类仍是小目标(pl20/p6/w13/pm30 AP<0.25)。 |
| 2026-06-16 | **`s_1280_sgd_e150_clean`** | **0.921** | **0.735** | 8.07M | — | **最终主实验（修正版）**；test 集，mP 0.901 / mR 0.850。1280+SGD lr=0.01+150轮+batch6，**全程不中断**。val mAP50 一路爬到 **0.935@e143**（曲线平滑无断崖，真饱和）。比旧 e40 阉割版 **+0.150**、比 baseline **+0.506(+122%)**。test(0.921)≈val(0.935)、三 split 文件名零重叠 → 无泄漏。**取代旧 `s_1280_sgd_e150` 为交付主模型**。详见「🔴 重大修正」。 |
| ~~2026-06-05~~ | ~~`s_1280_sgd_e150`~~ | ~~0.771~~ | ~~0.601~~ | 8.07M | — | ⚠️ **已废弃**（被 resume→640 bug 阉割）；test 集 mP 0.723 / mR 0.708。原以为「val 0.784@e40 即饱和」实为**假饱和**——e40 后续训误降 640、后 110 轮无效。被 `s_1280_sgd_e150_clean` 取代。 |
| 2026-06-11 | `s_640_sgd_e150` | **0.706** | **0.542** | 8.07M | 3.7 | test 集，mP 0.686 / mR 0.658。**v8 公平对比补充**：640+SGD 延长到 150 轮，val 0.728@e145 已基本饱和（e150=0.726），比 100 轮版 +0.060 但**仍低于 v8 同 640 的 0.798**。注：e20 处有一次人工中断+断点续训（resume 验证通过）。 |
| 2026-06-10 | `s_nomosaic` | 0.629 | 0.477 | 8.07M | 3.7 | test 集，mP 0.632 / mR 0.598。**增广消融**：仅全程关 mosaic，比对照 `s_640_sgd` 0.646 **−0.017**，val 峰值 0.625@e100（两组均未饱和、差距稳定）。证明 mosaic 对 TT100K 小目标正贡献（3.2.3）。 |
| 2026-06-05 | `s_1536_sgd` | 0.704 | 0.535 | 8.07M | — | **提分尝试（未达预期）**；test 集，mP 0.695 / mR 0.658。1536+SGD+100轮，但 batch 被迫降到 4。**val mAP50 单调爬升到 e100=0.673 仍未饱和**（对照 1280 batch8 在 e40 即 0.784），收敛慢一倍多 → 100 轮远不够，反低于 1280。根因是 **batch4 太小**（当时显存仅用 7.4G/12G，本可 batch8）。见下「batch 对高分辨率收敛的影响」。 |

### 🔴 重大修正（2026-06-16）：原「最终主实验 0.771」被 resume bug 阉割，修正版达 test 0.921

**起因**：复盘 `s_1280_sgd_e150` 曲线时发现 e40→e41 的断崖（val mAP50 0.784→0.55），查 checkpoint 的 `train_args` 坐实根因：

- `best.pt`(e40) `imgsz=1280`；`last.pt`(e150) `imgsz=640` 且 `resume=...last.pt`。
- e40 中断后用 `--resume` 续训，但**没带 `--imgsz 1280`**，而 `train.py --imgsz` 默认 640 → **后 110 轮实为 640 训练+验证**。TT100K 小目标在 640 下看不清，故 mAP 断崖、只回到 640 天花板（val ~0.756），**永远回不到 1280 峰值**。best.pt 因此仅含 40 个有效 1280 epoch。
- ⚠️ 这推翻了原「val 0.784@e40 即饱和、150 轮偏多」的判断——那是**假饱和**，是崩盘截断的错觉。

**修正实验** `s_1280_sgd_e150_clean`（1280 / SGD lr=0.01 / 150 轮 / batch6，全程不中断）：

| | 旧 `s_1280_sgd_e150`(e40, 阉割) | **新 `s_1280_sgd_e150_clean`** |
|---|:---:|:---:|
| test mAP@0.5 | 0.771 | **0.921** |
| test mAP@0.5:0.95 | 0.601 | **0.735** |
| test mR | 0.708 | **0.850** |
| val 峰值 | 0.784@e40（被截断） | **0.935@e143（未崩，真饱和）** |

- **可信度核验**：① test(0.921)≈val(0.935)，留出集与验证集吻合 → 真泛化非泄漏；② 三 split 文件名零重叠；③ 前 40 轮轨迹与旧 run 重合（e40 0.795 vs 0.784）→ 同起点同配置。
- **对项目结论的影响**：v10-S 在 1280 下 test mAP@0.5=**0.921**，**已超过 v8-S 640 的 0.798**。但 v8-S 仅在 640 测过，**公平对比需补 v8-S@1280**（新待办）。下方「公平对比补充」「最终主实验结论」「可视化」等基于 0.771 的论断均需据此重写（已就地加指针）。
- **教训**：`train.py` 的 `--resume` 路径不保留 imgsz（会落回默认 640），续训务必显式带 `--imgsz`；这本身是个值得在数据处理文档里写的「训练异常排查」案例。

### 增广消融：全程关 mosaic 掉 1.7 个点（3.2.3）
`s_nomosaic` 与 `s_640_sgd` 仅 `mosaic` 一项不同（其余 640+SGD lr=0.01+100 轮全同）：

| 配置 | test mAP@0.5 | mAP@.5:.95 | mP / mR | val 峰值 |
|------|:---:|:---:|:---:|:---:|
| mosaic 开（默认，末 10 轮关） | **0.646** | **0.494** | 0.610 / 0.623 | 0.653 @e99 |
| mosaic 全程关 | 0.629 | 0.477 | 0.632 / 0.598 | 0.625 @e100 |

- **mosaic 贡献 +0.017 mAP@0.5（+2.6%）**，两组 val 曲线 100 轮都未饱和、差距全程稳定 → 结论对训练时长不敏感。
- 机理与 TT100K 契合：mosaic 四图拼接相当于额外的尺度/上下文扰动，并把目标进一步缩小——对「小目标为主」的数据正向；同时增加单 batch 内目标数，缓解类别稀疏。
- 报告写法：作为 3.2.3 增广消融，与优化器（3.2.1）、分辨率（3.2.2）并列成三组单变量调参证据链。

### imgsz 640→1280 对比结论（核心实验）
**单改输入分辨率，test mAP@0.5 从 0.415 → 0.592（+43%）、mAP@0.5:0.95 从 0.302 → 0.457。** 直接证明 TT100K 的核心难点是小目标：640 下标志被压成几像素，1280 保留了细节。代价是显存/时间约 4×（batch 16→4，1.5h→6h）。
- 仍未到 0.70，进一步可尝试：显式 SGD lr=0.01（摆脱 auto 的保守 AdamW lr≈2e-4）、更高分辨率、或更长训练。

### 优化器发现：v10 baseline 低分是「欠拟合」，不是模型弱（核心）
横向对比暴露反常：同数据/同 640/同 100 轮，**YOLOv8-S=0.798 ≫ YOLOv10-S baseline=0.415**。逐项排查后定位根因在**训练**而非评测：
- **排除评测路径**：NMS 消融显示 v10 带 NMS(one2many) 也才 0.438，到不了 0.7，说明不是 one2one 评测压低了分。
- **收敛曲线铁证**：v10 baseline val mAP50 在 25/50/75/100 = 0.219/0.319/0.394/**0.421**，到 100 轮**仍线性爬升未饱和**，且 epoch1=0.000（预训练几乎没起步）；而 v8 第 25 轮就 0.692 接近饱和。典型欠拟合。
- **根因**：`optimizer=auto` 对 v10 选了保守的 AdamW（有效 lr≈2e-4），训练信号太弱。**显式 SGD lr=0.01 后**（`s_640_sgd`）640 下 test 0.415→**0.646(+56%)**，且曲线仍陡升（100 轮 val 0.651）——证实欠拟合假设。
- **结论**：公平对比 v10 vs v8 必须先让 v10 训练充分（显式 SGD + 足够 epoch）。当前 `s_640_sgd` 0.646 仍未饱和，加 epoch / 上 1280 预计可追平甚至超过 v8。**（后注 2026-06-11：150 轮实跑证伪了「同 640 加 epoch 可追平」——饱和于 0.706，仍低于 v8 0.798；上 1280 后 mAP@.5:.95 基本追平成立，见「公平对比补充」。）**

### 公平对比补充：640 下充分训练 v10 仍低于 v8（s_640_sgd_e150，3.3.1 修正）
「优化器发现」曾推测 `s_640_sgd`（100 轮未饱和 0.646）加 epoch 可追平 v8。实跑 150 轮：

| 模型 | 配置 | test mAP@0.5 | mAP@.5:.95 |
|------|------|:---:|:---:|
| YOLOv10-S | 640 SGD **150轮**（已饱和，val 0.728@e145） | 0.706 | 0.542 |
| YOLOv8-S | 640 auto 100轮 | **0.798** | **0.610** |

- **猜想证伪**：+50 轮只再涨 0.060，曲线已平，640 下 v10-S 距 v8-S 仍差 ~0.09 mAP@0.5——「欠拟合」只解释了 baseline 低分的大头（0.415→0.706），解释不了全部差距。
- **剩余差距的合理解读**（报告讨论点）：①mAP@0.5 差 0.092 而 mAP@.5:.95 只差 0.068，且 v10 的 mR 偏低（0.658 vs 0.732）——one2one 头每目标只出一框，在小目标/模糊目标上召回吃亏，这正是 NMS-free 拿延迟换的代价；②v8-S 参数量大 ~40%（11.2M vs 8.07M）；③TT100K 小目标密集，是对 one2one 召回最不利的场景。
- **公平结论链**（写报告用）：同 640 v10<v8（0.706 vs 0.798）→ 提到 1280 后 v10=~~0.771、mAP@.5:.95 基本追平（0.601 vs 0.610）~~且端到端推理更快（NMS-free）→ 复现结论是「精度可比 + 延迟优势」，与论文卖点一致，但在小目标数据上需要更高输入分辨率来补召回。
  - ⚠️ **2026-06-16 修正**：「1280 下 v10=0.771、仅基本追平 v8」基于被 resume bug 阉割的旧模型。修正版 `s_1280_sgd_e150_clean` 在 1280 下 test **0.921 / 0.735**，**反超 v8-S 640（0.798/0.610）**。但 v8 只在 640 测过，要下严谨结论需补 **v8-S@1280** 公平对比；在此之前，结论链应表述为「640 下 v10<v8；1280 下 v10 大幅提升至 0.921（v8 同分辨率待补）」。详见「🔴 重大修正」。
- 注：本实验 e20 处人工中断过一次，`--resume` 断点续训通过（曲线小扰动不影响饱和判断）。

### NMS-free 消融（任务 3.4.1，复现论文 Table 3 结论）
同一份权重（`s_base_640`，imgsz=640），切换推理头对比：

| 推理路径 | mAP@0.5 | mAP@0.5:0.95 | 推理ms | 后处理ms | 总ms |
|----------|:---:|:---:|:---:|:---:|:---:|
| 无NMS (one2one，默认) | 0.4153 | 0.3021 | 3.72 | **0.10** | 3.97 |
| 带NMS (one2many) | 0.4385 | 0.3237 | 3.63 | 0.69 | 4.51 |

**结论**：去掉 NMS 后 mAP 几乎无损（−0.023），后处理延迟从 0.69ms 砍到 0.10ms（**省 ~85%，约 7×**），端到端总延迟更低。直接验证 YOLOv10 的 NMS-free 设计「精度无损 + 推理更快」。复现命令：`python scripts/ablation_nms.py --weights runs/detect/s_base_640/weights/best.pt --imgsz 640`。

### 最终主实验结论（s_1280_sgd_e150，复现闭环）
叠加两个已验证杠杆——「攻小目标(1280)」+「修欠拟合(SGD lr=0.01)」+ 充分训练(150 轮)：

| 阶段 | 配置 | test mAP@0.5 | mAP@.5:.95 |
|------|------|:---:|:---:|
| baseline | v10 auto 640 | 0.415 | 0.302 |
| +1280 | v10 auto 1280 | 0.592 | 0.457 |
| +SGD | v10 SGD 640 | 0.646 | 0.494 |
| **最终(+1280+SGD+150轮)** | **v10 SGD 1280** | **0.771** | **0.601** |
| 对照 | v8 auto 640 | 0.798 | 0.610 |

- **从 baseline 0.415 → 0.771（+86%）**，越过及格线 0.70；
- **mAP@0.5:0.95 = 0.601，基本追平 YOLOv8 的 0.610（差 0.009）**；mAP@0.5 差 0.027。结合 NMS-free（精度无损、后处理快 ~7×），可论证「复现的 YOLOv10 精度持平 v8、端到端推理更省」——正是论文卖点。
- ~~**收敛特性**：1280+SGD 下 val mAP50 在第 40 轮即达峰值 0.784，后 110 轮未再提升 → 高分辨率下 40~50 轮已足够，150 轮偏多。~~
  - ⚠️ **2026-06-16 修正**：此「e40 饱和」是**假象**——后 110 轮被 resume bug 误降到 640 才停滞。全程不中断的 `s_1280_sgd_e150_clean` 显示 val 一路爬到 **0.935@e143**，**150 轮并不偏多**，模型远未在 e40 饱和。本节其余 0.771 相关数字均以「🔴 重大修正」为准。

### 提分尝试·负结果：TTA 与 NMS-free 不兼容（呼应 3.4.1）
对最终模型 `s_1280_sgd_e150/best.pt` 开启 TTA（测试时增强，多尺度+翻转，`eval.py --augment`）:

| 评测方式 | mAP@0.5 | mean Precision | mean Recall |
|----------|:---:|:---:|:---:|
| 普通 | **0.771** | 0.723 | 0.708 |
| TTA | 0.584 ⬇️ | **0.499 ⬇️** | 0.655 |

**TTA 不升反降（0.771→0.584），且 precision 暴跌而 recall 基本不变 → 产生大量重复假阳性框。** 根因：TTA 依赖 **NMS** 把多尺度/翻转的重复预测融合成一个;而 YOLOv10 是 **NMS-free**（one2one 头只做 top-k、无 NMS 去重），多套增强预测拼接后无法去重，导致重复检测。**这是架构特性,不是 bug**——与 NMS-free 消融（3.4.1）互为印证：one2one 头省了 NMS、降了延迟,但也**不兼容依赖 NMS 的后处理技巧（如 TTA）**。报告可作为「NMS-free 的优点与局限」讨论点。

### batch 对高分辨率收敛的影响（1536 提分尝试的教训）
试图用 1536 进一步攻小目标（`s_1536_sgd`），结果 test 0.704 反低于 1280 的 0.771。逐点对照 val mAP50：

| epoch | 1536 (batch4) | 1280 (batch8) |
|:---:|:---:|:---:|
| 40 | 0.473 | **0.784** |
| 60 | 0.581 | 0.638 |
| 80 | 0.645 | 0.681 |
| 100 | **0.673**（仍在爬） | 0.714 |

- **1536 收敛慢一倍多**：每个 epoch 都显著低于 1280 同期，到 e100 仍单调上升、未饱和；1280 在 e40 即冲到峰值。
- **根因是 batch=4**（1536 显存压力下从 8 降到 4），有效 batch 减半 → 梯度更新慢、收敛慢。**并非 1536 分辨率不好，而是 100 轮远没训够。**
- **教训**：高分辨率训练要么配足够 epoch（200+），要么在显存允许时尽量保 batch。实测当时 1536 batch4 显存仅用 7.4G/12G，**本可上 batch 8**（更快收敛）。报告可作为「训练配置（分辨率×batch×轮数）权衡」的讨论点。

### 结果整理与可视化（3.5，图表素材见 docs/figures/）
⚠️ **2026-06-16 更新**：本节图表原基于被阉割的 `s_1280_sgd_e150`（0.771），现已全部用修正版 `s_1280_sgd_e150_clean`（test **0.921**）重生成并覆盖同名文件（`training_curves.png` / `per_class_ap_s1280.{png,csv}` / `confusion_matrix_s1280.png` / `PR_curve_s1280.png` / `predict_demo/`×10）。下方旧描述中的数字（0.7709、最弱类等）已随之更新。产出：

- **训练曲线对比**（`training_curves.png`，`scripts/plot_curves.py`）：五实验 val mAP@0.5 同图——v8（25 轮近饱和） vs v10 auto（100 轮仍欠拟合） vs SGD/1280 各杠杆的收敛差异一目了然。注意 `s_1280_sgd_e150` 在 e40 峰值 0.784 后有一次骤降再回爬。~~（断点续训扰动所致，best.pt 恰取自 e40，不影响结论）~~ ⚠️ **2026-06-16 修正**：该骤降是 `--resume` 误把 imgsz 掉回 640 所致，**严重影响了结论**（模型被阉割、真实上限远高于 0.771）；报告图与每类 AP/混淆矩阵/PR 等均应改用修正版 `s_1280_sgd_e150_clean` 重新生成。详见「🔴 重大修正」。
- **每类 AP 柱状图 + CSV**（`per_class_ap_s1280.{png,csv}`，`scripts/plot_per_class_ap.py`，附 GT 实例数）：（clean 模型）强类 pr40/pl120/p27/il60/pl100 等 AP@0.5≈0.99；**最弱几类 wo(0.65,38例)/p6(0.78,39例)/ph4(0.82,37例)/w59/po**——已不再是小目标漏检为主，而是**兜底杂类**（wo「其他警告」、po「其他禁令」类别边界模糊）+ **少样本**类，绝大多数类已 0.8+。印证修正后瓶颈从「小目标」转向「类别歧义/长尾」。
- **混淆矩阵 / PR 曲线**（`confusion_matrix_s1280.png` / `PR_curve_s1280.png`）：val 时自动生成，已拷贝入库。
- **测试集检测可视化**（`predict_demo/`×10 张，`predict_demo.py --imgsz 1280`，2048²→1024 降采样入库）：含多目标路口、单小目标远景等场景。
- **训练标注抽查**（2.2.7，`scripts/draw_gt_samples.py`）：随机 20 张训练图叠加 GT 框肉眼复核，标注全部对齐（产物在 `runs/analysis/gt_samples/`，含一张 6 块龙门架限速牌图，可兼作「小目标难点」示意图）。

### 失败案例分析（3.5.4，`scripts/find_failure_cases.py`，图见 docs/figures/failure_cases/）
对最终模型在 test 集逐图做「同类 + IoU≥0.5」匹配（conf≥0.25）：**漏检 FN=1382、误检 FP=2370**（全集 ~14k 实例）。按类聚合 + 人工查看最差 top12 图，归纳三类失败模式：

1. **极小目标漏检**（rank02 收费站图，8 漏检全是远处收费亭上方的 pl20 限速牌）：目标远、像素少，1280 输入下仍不足以辨认。FN 绝对量被高频类主导（po 120 / pl50 110 / pl60 91），但相对弱类仍是每类 AP 图中的小尺寸限高/警告牌（ph5/w32/w13）。
2. **「类标志物」误检**（rank01 标志图例宣传牌被检出 11 个 FP；rank04 路口的信号灯/LED 倒计时屏被误检）：印在宣传牌上的标志图案、信号灯等视觉相似物触发检测——模型学到了「标志外观」但缺乏「真实路牌语境」判别。
3. **兜底类/近似类混淆**：FP 集中在 pn(221)/pl40(216)/po(199)/io(170)——po「其他禁令」、io「其他指示」本身是杂类，与具体类边界模糊；限速牌系列（pl30/40/50/60/80）数字仅一字之差，远距离低分辨率下互相误判（与混淆矩阵相互印证）。
   - 另有注意点：TT100K 只标 45 类子集，画面中真实存在但不属于 45 类（或漏标）的标志被检出也计为 FP——部分「误检」实为**标注不完备**，报告分析时应说明。

> 改进方向（写报告用，不必全做）：更高分辨率/切片推理攻极小目标；困难负样本（宣传牌、信号灯）增强；合并兜底类或层级分类缓解近似类混淆。

### baseline 关键发现
- **未达及格线 0.70**：根因是 640 把 2048² 原图的小标志压没了（TT100K 标志常仅几十像素）。
- **`optimizer=auto` 覆盖了 `--lr0`**：实际用 AdamW lr≈0.0002（偏保守）。要控 lr 需显式 `optimizer=SGD`。
- 改进优先级：① **imgsz 1280**（对小目标最有效，batch 需降到 4）② 显式 SGD + lr=0.01 ③ 关 mosaic 末段微调。

## 复现命令速查

```powershell
conda activate yolov10

# 主实验
python scripts/train.py --model yolov10s --name s_base_640 --epochs 100 --batch 16
python scripts/eval.py  --weights runs/detect/s_base_640/weights/best.pt
python scripts/predict_demo.py --weights runs/detect/s_base_640/weights/best.pt --n 10

# 对比 YOLOv8-S
python scripts/train.py --model yolov8s.pt --name v8s_base_640 --epochs 100 --batch 16
python scripts/eval.py  --weights runs/detect/v8s_base_640/weights/best.pt
```

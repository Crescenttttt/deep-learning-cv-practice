# 任务拆解 · YOLOv10 + TT100K 复现路径

> **本文目的**：把"复现 YOLOv10 并在 TT100K 上做迁移微调"这件事，拆成**按时间顺序、可勾选、带验证标准**的子任务清单，3 人小组照着推进即可。
> **关系**：本文是**执行清单（怎么做）**；高层路线与学习重点见 `项目完成路径与学习指南.md`；论文层面的深度分析见 `文献总结-02-YOLOv10（深度分析）.md`。
> **当前进度（2026-05-28）**：W11 调研 ✅ / W12 开题 PPT ✅ / 正进入 **W13 需求分析阶段**。

---

## 角色代号（沿用学习指南的 3 人分工）

| 代号 | 角色 | 主责 |
|------|------|------|
| **A** | 组长 / 算法主讲 | YOLOv10 论文与深度分析、答辩主讲 |
| **B** | 工程 / 训练 | 跑通官方代码、训练脚本、调参、对比/消融实验执行 |
| **C** | 数据 / 文档 | TT100K 下载/清洗/格式转换/划分、实验记录、文档撰写、PPT 与图表 |

> 文档由全员共同完善，代号只是**主责人**，不是隔离。3 人组比 4 人多承担约 33% 工作量，**严格对齐周节点**比 4 人组更重要——任何环节卡 3 天必须组会拉齐。

---

## 阶段 0 · 启动校验（本周内，1–2 天）

> **目标**：确保选题、文献、工具链都没有阻塞项，避免下游返工。

| # | 任务 | 主责 | 验证标准 |
|---|------|:---:|----------|
| 0.1 | 复核 4 篇文献全部为 **2023 年以来**发表 | A | 在 `papers/README.md` 列出每篇的发表年份与会议/期刊；全部 ≥ 2023 |
| 0.2 | 锁定 YOLOv10 论文为「深度分析」对象 | A | 在开题 PPT 与需求分析报告中显式声明 |
| 0.3 | 确认每人都有可访问的 Python 环境（本地 / Colab / Kaggle）；明确 GPU 归属 | B | 列一张表：成员 / 是否有本地 GPU / 显存 / 备用方案 |
| 0.4 | 注册 / 登录 TT100K 官方下载渠道，确认下载可行 | C | 在本地或共享盘上能拿到 `data.zip` 或对应链接 |
| 0.5 | 建立小组协作渠道（文档同步 / 代码仓库权限 / 周会时间） | C | 每人都能 push 到 main 分支并编辑文档 |

---

## 阶段 1 · 需求分析（W13–14 → 交付《需求分析报告》）

> **目标**：把"做什么、用什么数据、走什么技术路线、怎么评"彻底定下来，**写成报告**。
> 课程评分：期末 30%（个体 10 + 团队 20），权重最大的一档，**务必认真写**。

### 1.1 数据集分析（C 主责，A 配合）

- [ ] **1.1.1** TT100K 数据集统计：总图像数、原图分辨率、类别数、标注格式（JSON）、官方划分
- [ ] **1.1.2** 类别分布直方图，**显式列出样本数 < 100 的稀有类**
- [ ] **1.1.3** 决定子集策略：取样本数 ≥ 100 的 **45 类子集**（业界通用做法），并在报告中给出**为什么裁剪**的论证
- [ ] **1.1.4** 数据集"难点"小节：**小目标**（标志在 2048² 原图中只占几十像素，需要量化：bbox 平均像素面积、< 32×32 的占比）、**类别不平衡**（最大类 vs 最小类样本比）
- [ ] **1.1.5** 划分方案：train / val / test = 7 / 1.5 / 1.5（或沿用官方划分，需说明）

**验证**：报告中有数据集概览表、类别分布图、小目标占比统计、划分表格四件套。

### 1.2 系统功能与技术路线（A 主责，B 配合）

- [ ] **1.2.1** 系统输入输出定义：输入 RGB 图像 / 视频帧 → 输出 [类别 + bbox + 置信度]
- [ ] **1.2.2** 技术路线图：数据 → YOLOv10 训练 → 评测 → 可视化（一张框图）
- [ ] **1.2.3** 选定模型规模：**YOLOv10-N**（轻量、Colab 可跑）或 **-S**（精度更好，建议主选）
- [ ] **1.2.4** 训练策略：加载官方 COCO 预训练权重 → 在 TT100K 子集上 **fine-tune**
- [ ] **1.2.5** 对比方案：YOLOv10-S **vs** YOLOv8-S（baseline）**vs** RT-DETR（同期工作）

**验证**：报告中有"系统框图 + 模型选型表 + 对比方案表"。

### 1.3 评价指标与成功标准（A 主责，C 配合）

- [ ] **1.3.1** 主指标：mAP@0.5、mAP@0.5:0.95（COCO 标准）
- [ ] **1.3.2** 辅指标：每类 AP、参数量、FLOPs、单图推理延迟（ms）
- [ ] **1.3.3** **设定及格线**（用于第 3 阶段验收）：例如 mAP@0.5 ≥ 0.70（参考已有 TT100K + YOLOv8 类工作的合理区间，**先调研再定**）
- [ ] **1.3.4** 可视化标准：随机抽 10 张测试图给出检测结果对比图

**验证**：报告中明确"达到什么数字算成功"。

### 1.4 风险与应对（C 主责）

- [ ] **1.4.1** 沿用 `项目完成路径与学习指南.md` 第六节风险表，针对 TT100K 补充：**小目标漏检风险**、**稀有类过拟合风险**、**2048² 高分辨率训练显存风险**
- [ ] **1.4.2** 对每条风险给出应对（如显存不足 → 切到 -N 规模 / 切到 640×640 输入 / 用 Colab Pro）

### 1.5 报告撰写与定稿（C 主责，全员校对）

- [ ] **1.5.1** 按学校模板撰写《需求分析报告》（封面、目录、正文、引用、附录）
- [ ] **1.5.2** 引用 4 篇文献，引用格式统一（GB/T 7714 或 IEEE，**选定一种用到底**）
- [ ] **1.5.3** 全员校对一遍 → 定稿提交

**阶段 1 出口标准**：《需求分析报告》PDF + 源文件 commit 入仓库，组员每人能口头讲清楚"我们要做什么、用什么数据、怎么评"。

---

## 阶段 2 · 概要设计（W15–16 → 交付《概要设计文档》）

> **目标**：环境跑通、官方代码跑通、TT100K 数据准备好、训练/评测脚本设计完成。
> 这阶段**是不是写代码**的关键期——失败往往败在环境与数据。

### 2.1 环境搭建（B 主责）

- [x] **2.1.1** Clone 官方仓库 `git clone https://github.com/THU-MIG/yolov10`（克隆到 `D:\Project\yolov10`）
- [x] **2.1.2** 创建 conda 环境（Python 3.9 / PyTorch 2.0.1+cu118 / RTX 4070 12GB），用 conda-forge 源
- [x] **2.1.3** 验证：`yolov10n` 对 `bus.jpg` 推理通过，检出 4 persons + 1 bus，GPU ~68ms/图
- [x] **2.1.4** 已写成 `docs/环境搭建.md`（含 5 个踩坑记录）

**验证**：至少 2 名成员（B + 另 1 人）独立按文档跑通推理。 → ⏳ 首搭机（B）已通过，待第 2 人复现。

### 2.2 TT100K 数据准备（C 主责）

- [x] **2.2.1** 下载 TT100K 原始数据（2016 版，已放 `D:\Project\data`）
- [x] **2.2.2** 解压、整理目录、检查图片可读（含 `train/ test/ other/`，`annotations.json` 16811 图）
- [x] **2.2.3** **类别过滤**：`scripts/filter_tt100k.py` 已写——从标注统计各类实例数，按 `--min-count`（默认 100）筛选，输出 `tt100k_classes.txt` + 全类别 `counts.csv`（供 1.1.2 直方图），并与论文标准 45 类交叉核对。已用合成数据冒烟测试通过。
- [x] **2.2.4** **格式转换**：`scripts/tt100k_to_yolo.py` 已写——TT100K JSON → YOLO `.txt`（`class_id cx cy w h`，按各图实际尺寸归一化）。已验证非正方形图 W/H 分别归一化正确。
- [x] **2.2.5** **数据划分**：同上脚本完成——沿用官方 train/test（按 path 前缀），再从 train 切出 val（`--val-ratio` 默认 0.1）；采用 `images/{train,val,test}` + `labels/{...}` 标准目录结构（非 list 文件，等价且更通用），图像默认硬链接零拷贝。
- [x] **2.2.6** 生成 Ultralytics 风格 `tt100k.yaml`（`path` / `train` / `val` / `test` / `names`）——同上脚本自动产出，已验证。
- [ ] **2.2.7** 抽样可视化：随机 20 张训练图叠加 bbox，肉眼验证标注无误

> **脚本状态**：2.2.3-2.2.6 已在**真实 TT100K（2016 版）实跑通过**：
> - 统计：16811 图 / 182 类 / 26349 实例 → 阈值 100 筛出 **45 类**（保留 90.8% 实例），**与论文标准 45 类完全一致**；
> - 转换/划分：**train/val/test = 5493 / 610 / 3067**，0 缺失 0 丢弃，硬链接零拷贝；
> - 加载校验：`check_det_dataset('datasets/tt100k/tt100k.yaml')` 通过（45 names）；并跑通 1-epoch 训练（见 2.3）。
> - 产物：`scripts/tt100k_classes.txt`（45 类，定义 class_id 顺序）、`scripts/tt100k_class_counts.csv`（全类计数，供 1.1.2 直方图）、`datasets/tt100k/`（已 gitignore）。
>
> 复现命令：
> ```powershell
> conda activate yolov10
> python scripts/filter_tt100k.py --ann D:\Project\data\annotations.json
> python scripts/tt100k_to_yolo.py --data-root D:\Project\data
> ```

**验证**：✅ 已通过 `check_det_dataset` 加载校验 + 1-epoch 训练不报错。

### 2.3 训练 / 评测脚本设计（B 主责，A 协助理解超参）

- [x] **2.3.1** `scripts/train.py`：封装 `model.train()`，支持本地权重/HF id/简写，暴露 epochs/imgsz/batch/lr0/close-mosaic；输出固定到 `runs/detect/<name>`。
- [x] **2.3.2** `scripts/eval.py`：在指定 split 算 mAP@0.5、mAP@0.5:0.95、mP/mR + 每类 AP（升序列出便于定位弱类）。
- [x] **2.3.3** `scripts/predict_demo.py`：test 集随机抽 N 张或指定 source 推理画框出图。
- [x] **2.3.4** 实验配置表 `experiments.md`：baseline / lr / imgsz / 增广 / YOLOv8-S 对比 / -N 共 7 组计划 + 命令速查。
- [x] **2.3.5** 日志规范：训练统一输出 `runs/detect/<name>/`，name 与 `experiments.md` 行对齐。

> 三脚本已 Python 3.9 编译 + `--help` 验证；并用 **1-epoch 真实训练打通 train→val→存 best.pt 全链路**（YOLOv10s/45类/AdamW；1 epoch 指标≈0 属正常）。修正了输出落到 ultralytics 全局目录的问题（现固定写仓库 `runs/detect/`）。

**验证**：✅ 1-epoch 短训跑通；正式 5+ epoch 训练见阶段 3.1。

### 2.4 系统设计文档（C 主责）

- [ ] **2.4.1** 撰写《概要设计文档》：模块图（数据预处理 → 模型 → 评测 → 可视化）、关键脚本说明、数据流图、目录结构
- [ ] **2.4.2** 包含一份「环境与数据复现指南」，让助教/老师能照着跑

**阶段 2 出口标准**：在 5 epoch 短训上能跑出非空 mAP 数字 + 一组可视化检测图 + 文档定稿。

---

## 阶段 3 · 模型调试与实验（W17–18 → 交付《数据处理设计文档》）

> **目标**：跑出**正经的训练曲线**、**完成对比与消融**、把结果**装进文档与 PPT**。

### 3.1 基础训练（B 主责）

- [x] **3.1.1** 主实验已跑：YOLOv10-S + COCO 预训练，imgsz=640，batch=16，100 轮 → `runs/detect/s_base_640/`
- [x] **3.1.2** 监控：loss/mAP/lr 曲线已出（`results.png`、`confusion_matrix.png`、`PR_curve.png` 等）
- [x] **3.1.3** test 集报告：**mAP@0.5=0.415，mAP@0.5:0.95=0.302**，mP 0.427 / mR 0.445，4.2ms/图（见 `experiments.md`）
- [x] **3.1.4** 验收：**未达及格线 0.70 → 进入调参**（小目标瓶颈，下一步 imgsz=1280，见 3.2）

### 3.2 调参实验（B 主责，A 提供理论判断）

> 不必穷举，挑 **2–3 组**有理由的对比即可，目的是**写进报告**。

- [ ] **3.2.1** 学习率：初始 lr=0.01 vs 0.005 vs 0.001（cosine schedule）
- [ ] **3.2.2** 输入分辨率：640 vs 800（TT100K 小目标多，**高分辨率通常更友好**，需验证）
- [ ] **3.2.3** 数据增广：默认 mosaic+mixup vs 关闭 mosaic 最后 10 epoch（论文常见做法）
- [ ] **3.2.4** 每组实验填入 `experiments.md` 对比表（实验名 / 配置 / mAP / 备注）

### 3.3 横向对比（B + A）

- [ ] **3.3.1** **YOLOv10-S vs YOLOv8-S**：同数据、同 epoch、同 imgsz，比 mAP / 参数 / 延迟
- [ ] **3.3.2** **YOLOv10-S vs RT-DETR-R18**（可选，时间紧可省）
- [ ] **3.3.3** 输出对比表（参考论文 Table 1 样式）

### 3.4 纵向消融 / 设计验证（A 主责，B 配合）

> 全面复现论文消融成本高，**挑 1–2 个有代表性的**做即可。

- [ ] **3.4.1** **NMS-free 验证**：分别测「带 NMS（一对多头）」与「无 NMS（一对一头）」的推理延迟与 mAP，验证论文 Table 3 的结论
- [ ] **3.4.2** **(可选) SCDown 消融**：把 SCDown 替换回传统 3×3 stride-2 conv，对比参数/延迟/精度（需要改模型代码，**仅在时间充裕时做**）

### 3.5 结果整理与可视化（C 主责）

- [ ] **3.5.1** 训练曲线图（loss / mAP，导出 PNG）
- [ ] **3.5.2** 测试集随机 10 张图的检测可视化（**含一张小目标 hard case**）
- [ ] **3.5.3** 混淆矩阵 / PR 曲线 / 每类 AP 柱状图
- [ ] **3.5.4** 失败案例分析：找 2–3 张漏检 / 误检图，文字分析原因（一般是小目标、稀有类、光照）

### 3.6 文档撰写（C 主责）

- [ ] **3.6.1** 撰写《数据处理设计文档》：数据预处理流程、训练超参、实验结果、对比/消融、失败分析
- [ ] **3.6.2** 与阶段 1/2 文档保持术语一致

**阶段 3 出口标准**：主实验 mAP 达标 + 至少 1 组调参对比 + 1 组横向对比 + 1 组消融 + 文档定稿。

---

## 阶段 4 · 总结汇报（W18 → 交付总结 PPT）

| # | 任务 | 主责 |
|---|------|:---:|
| 4.1 | 总结 PPT（建议 15±5 页：背景 / 数据 / 模型 / 训练 / 结果 / 消融 / 演示 / 总结） | C + A |
| 4.2 | 演示视频或现场 demo（推理一张图） | B |
| 4.3 | 答辩问答准备：每人能讲清楚自己负责模块 | 全员 |
| 4.4 | 仓库整理：README 完整、脚本可一键复现、`.gitignore` 屏蔽大文件 | B + C |

---

## 关键依赖关系（速查）

```
0.4(下载) ──► 2.2(数据准备) ──┐
                              ├──► 2.3(脚本) ──► 3.1(主训练) ──► 3.2/3.3/3.4(实验) ──► 3.5/3.6(整理)
0.3(GPU) ──► 2.1(环境) ───────┘                                                        │
                                                                                       └──► 4.1(PPT)
1.x(需求分析) 与 2.x(概要设计) 主要是文档线，**与代码线并行**，不要互相阻塞。
```

**Tips**：
- **代码线**（B、C 主导）可以从阶段 0 就开始动手，不必等阶段 1 报告写完。
- **文档线**（A、C 主导）持续输出，每阶段一次定稿提交。
- 每周固定一次组会，对照本文档勾选进度，红灯项立刻拉群讨论。

---

## 备忘 · 不要忘记的小事

- [ ] 仓库根 `.gitignore` 屏蔽：`runs/`、`*.pt`、`datasets/`、`__pycache__/`、`.idea/`、`.vscode/`
- [ ] 大数据集和权重**不要 commit**，用云盘/学校盘共享，仓库里只放路径说明
- [ ] 所有 markdown 文档统一编码 UTF-8、统一行结尾 LF（防止 Windows/Mac 混编时 diff 噪音）
- [ ] 引用论文格式选定一种（GB/T 7714 或 IEEE），**一以贯之**

---

*文档生成日期：2026-05-28*

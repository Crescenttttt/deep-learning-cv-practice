# 基于深度学习的计算机视觉实践项目

## 项目性质

CUIT「基于深度学习的计算机视觉实践项目」课程作业，2026 春第 11–18 周，计科 231–234 班，授课教师汪曼青（wangmanqing@cuit.edu.cn）。课程默认 4 人/组，**本组实际 3 人**。要求综合运用图像处理与深度学习，围绕**识别 / 分割 / 分类**完成一个应用系统的主体设计。

## 本组选题（已确定）

- **方向 / 场景**：目标检测，智能驾驶 —— 交通标志检测
- **数据集**：TT100K（清华-腾讯，原图 2048×2048，200+ 类，通常取样本 ≥ 100 的 ~45 类子集训练）
- **核心模型**：**复现 YOLOv10**（Wang et al., NeurIPS 2024，无 NMS、双标签分配、Rank-guided block design / PSA）
- **路线**：开题"二选一"中的 **方案 1 —— 复现已有文献模型**
- **官方仓库**：https://github.com/THU-MIG/yolov10

## 课程进度（周次任务）

| 周次 | 任务 | 交付物 |
|------|------|--------|
| 第 11 周 | 调研：选方向、收数据集、读 4 篇 2023+ 文献 | — |
| 第 12 周 | 开题报告（PPT 5 分钟） | 开题 PPT |
| 第 13–14 周 | 需求分析（数据集、模型、技术路线） | 需求分析报告 |
| 第 15–16 周 | 概要设计（环境、跑通代码、准备数据） | 概要设计文档 |
| 第 17–18 周 | 模型调试（设计、训练、度量、迭代） | 数据处理设计文档 |
| 第 18 周 | 总结汇报 | 总结 PPT |

## 开题与文献要求（来源：`开题报告及文献分析要求.pptx`）

- **选题背景**：识别 / 检测是什么，应用场景与意义，可选方法 / 模型。
- **文献总结**：4 篇 **2023 年以来** 的论文简析 + 从中选 1 篇做**深度分析**。
- **深度分析需含**：模型框架（重点）、数据集划分、参数设置、训练过程、评价指标、有效性分析（消融实验）。
- **拟采用模型（二选一）**：①复现文献模型；②自行设计并与经典模型对比。

## 考核占比（总评 100%）

- 平时 30%：出勤 10% + 开题 10%（个 5 + 团 5）+ 中期 10%
- 期末 70%：需求分析 30%（10+20）+ 概要设计 20%（10+10）+ 数据处理设计 20%（10+10）

## 技术与资源

- **前序课程**：数字图像处理、计算机视觉、机器学习、工程实践 3
- **语言 / 硬件**：Python 为主（也允许 MATLAB），小组至少 1 台 GPU 机
- **检索渠道**：Kaggle、Papers with Code、GitHub、CUIT 数字图书馆（SpringerLink / IEEE Xplore / ACM DL / WoS / Engineering Village / CNKI）
- **经典算法（背景）**：LeNet · AlexNet · YOLO · FCN / U-Net · Neural Style · FaceNet
- **典型流程**：数据获取 → 清洗 / 准备 → 建模（设计 / 训练 / 度量 / 迭代）→ 交付

## 项目当前状态

工作目录 `D:\Project\deep-learning-cv-practice`（Git 仓库已建立，main 分支）。

**已完成**：仓库与根 README + 4 篇文献总结（`a39bf0d`）；选题已定（YOLOv10 + TT100K）；**环境搭通**（conda `yolov10`，Py3.9 + PyTorch2.0.1+cu118，RTX4070；官方仓库 `D:\Project\yolov10`；`docs/环境搭建.md`，任务 2.1）；**TT100K 数据准备完成**（`scripts/filter_tt100k.py`+`tt100k_to_yolo.py`，真实 2016 版实跑：182→45 类与论文一致，train/val/test=5493/610/3067，`check_det_dataset` 通过；`docs/数据下载与目录说明.md`，任务 2.2）；**训练/评测脚本交付**（`scripts/train.py`/`eval.py`/`predict_demo.py`+`experiments.md`，任务 2.3）；**YOLOv10-S baseline 已跑**（640/100轮，test **mAP@0.5=0.415**，任务 3.1）。**调参/对比/消融已推进**：①`imgsz=1280` → **0.592**（攻小目标，3.2.2）；②**优化器修复**：发现 baseline 低分是 `auto`(AdamW lr≈2e-4) 欠拟合，改 **显式 SGD lr=0.01** → 640 下 **0.646(+56%)**，且 val 曲线 100 轮仍未饱和（`s_640_sgd`，3.2.1）；③**横向对比** YOLOv8-S=**0.798**（同 640，揭示 v10 欠拟合，3.3.1）；④**NMS-free 消融**（`scripts/ablation_nms.py`）：无NMS vs 带NMS 精度近无损、后处理省~85%(~7×)，复现论文 Table 3（3.4.1）。原始数据在仓库外 `D:\Project\data`。

**下一步**：跑**最终主实验**——`1280 + 显式 SGD lr=0.01 + 150~200 轮`（叠加「攻小目标」+「修欠拟合」两个已验证杠杆，目标 0.8+）；抽样可视化（2.2.7/3.5.2）；失败案例分析（3.5.4）；推进需求分析报告等文档（期末占比最大）。脚本变更：`train.py` 已加 `--optimizer`，`eval.py` 已按权重头类型自动选 YOLO/YOLOv10（否则 v8 评测会错）。

## 给 Claude 的协作提示

- **定位**：学生课程项目，重「流程完整 + 文档规范」，不是发顶会。优先复现成熟开源工作，文档（开题 / 需求 / 概要 / 总结 PPT）是核心交付物，代码是辅助。
- **文献**：必须 2023 年以来，每组 4 篇，引用规范。
- **默认路线**：后续协作默认在 **YOLOv10 + TT100K** 内推进，除非用户明说换题。讲网络 / 消融 / 训练参数时优先围绕 YOLOv10 关键设计（双标签分配、无 NMS、Rank-guided block、PSA）。
- **TT100K 两个核心陷阱**（开题 / 需求 / 概要文档里必须交代）：
  - **小目标**：标志在 2048×2048 原图中只占几十像素；
  - **严重类别不平衡**：200+ 类，多数类样本 <100，故业界通用做法取 45 类子集。
- **任务完成后必须更新文档**：每当完成一个有实质产出的任务（写代码、跑实验、改设计、定决策），立即同步更新相关文档——
  - 勾选 `docs/任务拆解-YOLOv10-TT100K复现路径.md` 中对应的 checkbox（`- [ ]` → `- [x]`），保证清单状态实时反映进度；
  - 若任务产生新决策 / 数字 / 路径 / 脚本，同步更新 `CLAUDE.md`「项目当前状态」、`docs/项目完成路径与学习指南.md`、阶段对应的需求 / 概要 / 数据处理文档；
  - 不要等"阶段末尾再补"——回头补文档的代价远高于即时记录。

## Git 协作准则（强制）

> 适用对象：所有组员 + Claude。**任何代码或文档变更都不允许直接推 `main`，必须走分支 + PR**。

- **禁止操作**：
  - `git push origin main`（直推 main）
  - `git push --force` / `git push -f`（强制推送，**任何分支都不允许**，除非用户明确授权）
  - 在 `main` 分支上 commit（即使本地）
- **标准流程**：
  1. `git checkout main && git pull` 同步最新；
  2. 新建分支：命名规范 `<type>/<short-desc>`，type ∈ `feat` / `fix` / `docs` / `data` / `exp` / `chore`，例如 `docs/requirement-analysis`、`exp/yolov10s-baseline`、`data/tt100k-45cls`；
  3. 在分支上提交（commit message 用中文 + 动词开头，如「新增 TT100K 类别过滤脚本」「修正 mAP 计算」）；
  4. `git push -u origin <branch>`；
  5. 用 `gh pr create` 或 GitHub UI 提交 PR，**至少 1 名其他组员 review 通过**后合入；
  6. 合入方式默认 **Squash merge**（保持 main 历史整洁），合入后删除分支。
- **Claude 的额外约束**：
  - 提交前先 `git status` + `git branch --show-current` 确认当前不在 `main`；如在 main，先建分支再 commit；
  - **绝不**在未取得用户明确确认时执行 `git push`、`gh pr merge`、`git push --force` 等远程/破坏性操作；
  - 即便用户说"提交一下"，也应理解为"在分支上 commit"，而不是直接合入 main。

## 行为准则

### 1. 先思考，再编码

**不要臆测。不要隐藏困惑。把权衡摆到台面上。**

实现之前：

- 明确陈述你的假设。若不确定，先提问。
- 若存在多种解读，全部列出——不要默默选定一种。
- 若存在更简单的方案，说出来。必要时提出反对意见。
- 若有不清楚的地方，停下来。指出困惑所在。提问。

### 2. 简洁优先

**用解决问题的最少代码。不做任何投机性设计。**

- 不实现超出需求范围的功能。
- 不为一次性代码做抽象。
- 不添加未被要求的「灵活性」或「可配置性」。
- 不为不可能发生的场景做错误处理。
- 若写了 200 行而其实 50 行就够，重写它。

自问：「资深工程师会觉得这过度复杂吗？」若是，就简化。

### 3. 外科手术式修改

**只改你必须改的。只清理你自己制造的烂摊子。**

修改既有代码时：

- 不「顺手优化」相邻代码、注释或格式。
- 不重构没坏的东西。
- 沿用既有风格，即便你会用别的写法。
- 若发现无关的死代码，提出来——不要删除它。

当你的改动产生了孤儿代码时：

- 移除因你的改动而不再被使用的 import / 变量 / 函数。
- 除非被要求，不要移除原本就存在的死代码。

检验标准：每一行被修改的代码都应能直接追溯到用户的需求。

### 4. 目标驱动执行

**定义成功标准。循环直至验证通过。**

把任务转化为可验证的目标：

- 「加校验」→「为非法输入写测试，再让它们通过」
- 「修复 bug」→「写一个复现该 bug 的测试，再让它通过」
- 「重构 X」→「确保重构前后测试都通过」

对于多步骤任务，先简述计划：

```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

强的成功标准让你能独立循环推进；弱的标准（「让它能用」）则需要不断澄清。
